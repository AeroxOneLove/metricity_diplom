events {}

http {
    server {
        listen 80;
        server_name _;

        location /media/ {
            alias /media/;
            try_files $uri =404;
            add_header Access-Control-Allow-Origin "*" always;
        }

        location / {
            return 404;
        }
    }
}
